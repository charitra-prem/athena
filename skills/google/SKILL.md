# google

**STATUS: not yet wired.** Gmail/Calendar/etc. via this skill will land when a
Gmail source is added to `/api/sources/`. Today this directory is a stub — if
you see an event with `source: "gmail-*"`, the system isn't ready to handle it
yet.
